$mera = $mera || {};
$mera.entityGenerators = $mera.entityGenerators || [];

$mera.<%= model.modelName.toLowerCase() %>Generator = function(nga) {
  var entity = nga.entity('<%= entityUrl %>').baseApiUrl('<%= baseApiUrl %>').label('<%= entityLabel%>');
  entity.listView().fields([
  ]);
  entity.creationView().fields([
  ]);
  entity.editionView().fields([
  ]);
  return entity;
};

$mera.entityGenerators.push($mera.<%= model.modelName.toLowerCase() %>Generator);